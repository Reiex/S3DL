#pragma once

#include <S3DL/types.hpp>
#include <S3DL/PipelineLayout.hpp>

namespace s3dl
{
    template<typename T>
    void PipelineLayout::setGlobalUniforms(uint32_t binding, T* values, uint32_t count, uint32_t startIndex)
    {

    }
}
