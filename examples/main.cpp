#include "main.hpp"

int main()
{
    int exit_code = 0;
    exit_code = main_viking_room();
    // exit_code = main_calibration();
    // exit_code = main_geckos();

    system("pause");

    return exit_code;
}
